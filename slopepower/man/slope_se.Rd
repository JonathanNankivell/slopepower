% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{slope_se}
\alias{slope_se}
\title{Standard error of the estimated slope}
\usage{
slope_se(params)
}
\arguments{
\item{params}{A \code{slope_params} object produced by \code{\link[=slope_params]{slope_params()}}. Objects
from \code{\link[=slope_params_manual]{slope_params_manual()}} carry no fitted model, so \code{NA_real_} is
returned for them.}
}
\value{
A single non-negative number, or \code{NA_real_} if \code{params} has no
fitted model, or if the slope terms could not be identified in it (with
a warning).
}
\description{
The standard error of the untreated (or case) slope, taken from the fitted
mixed model's fixed-effects covariance matrix: for \code{comparator = "healthy"}
or \code{"treated"} this combines the variances of, and covariance between, the
two fixed-effect terms whose sum is the slope, not just the variance of a
single coefficient. \code{\link[=slope_bootstrap]{slope_bootstrap()}} compares this to the slope itself
as the check recommended in section 2.6 of Nash et al. (2021): a slope
less than 2.5 times its standard error means bootstrap replicates can
straddle zero, at which point the resulting interval stops meaning
anything.
}
\examples{
pars <- slope_params(sdmt ~ visit | id, data = slpower1)
slope_se(pars)

# slope_params_manual() objects carry no fitted model, so there is no
# standard error to report.
slope_se(slope_params_manual(
  slope = -1.672, sigma2_intercept = 100, sigma2_slope = 2,
  sigma_cov = 5, sigma2_residual = 10
))

}
\seealso{
\code{\link[=slope_sigma]{slope_sigma()}} and \code{\link[=slope_var]{slope_var()}}, the other quantities computed
from a \code{slope_params} object; \code{\link[=slope_bootstrap]{slope_bootstrap()}}, which uses this to
flag an unreliable interval.
}
