% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compat.R
\name{slopepower}
\alias{slopepower}
\title{Sample size or power using the Stata command's interface}
\usage{
slopepower(
  data,
  depvar,
  subject,
  time,
  schedule,
  obs = FALSE,
  rct = FALSE,
  nocontrols = FALSE,
  casecon = NULL,
  treat = NULL,
  dropouts = NULL,
  scale = 1,
  alpha = 0.05,
  power = NULL,
  n = NULL,
  effectiveness = NULL,
  usetrt = FALSE,
  ...
)
}
\arguments{
\item{data}{A data frame in long format with repeated measurements.}

\item{depvar, subject, time}{Column names, as strings.}

\item{schedule}{Numeric vector of follow-up visit times for the planned trial,
in units of \code{time / scale}. Baseline at time 0 is implicit.}

\item{obs, rct}{Declare the data as observational or as a previous trial.
Exactly one must be \code{TRUE}.}

\item{nocontrols}{Use with \code{obs} when every subject has the condition.}

\item{casecon}{Column name identifying cases (1) and healthy controls (0). Used
with \code{obs}.}

\item{treat}{Column name identifying the treated arm (1) and control arm (0).
Used with \code{rct}.}

\item{dropouts}{Numeric vector of incremental dropout proportions aligned with
\code{schedule}: element \code{j} is the proportion whose last attended visit is
\code{schedule[j]}. They are handled by the Dawson and Lagakos (1991, 1993)
pattern mixture, as in the Stata original; see \code{\link[=trial_design]{trial_design()}}.}

\item{scale}{Number of \code{time} units in one \code{schedule} unit. Defaults to 1.}

\item{alpha}{Two-sided significance level. Defaults to 0.05.}

\item{power, n}{Supply one, as in Stata. \code{n} gives the power that sample size
achieves; \code{power} gives the sample size that reaches it. \code{power} defaults to
0.8 when neither is given.}

\item{effectiveness}{Proportion of the slope difference the treatment should
remove. Defaults to 0.25. Mutually exclusive with \code{usetrt}.}

\item{usetrt}{Target the treatment effect observed in the supplied trial data.
Requires \code{rct}.}

\item{...}{Passed to \code{\link[=slope_params]{slope_params()}}, e.g. \code{common_variance}.}
}
\value{
A \code{slope_sample_size} object, or a \code{slope_power} object when \code{n} is
supplied. Both inherit from \code{slope_result}.
}
\description{
A direct translation of the Stata \code{slopepower} command of Nash et al. (2021)
onto this package's pipeline. Argument names, defaults and conventions follow
the Stata original: \code{schedule} lists follow-up visits only with baseline assumed
at time 0, \code{scale} divides the time variable, \code{dropouts} aligns element by
element with \code{schedule}, and the data type is declared with \code{obs}/\code{rct} rather
than by which grouping variable is supplied.
}
\details{
New code should use \code{\link[=slope_params]{slope_params()}}, \code{\link[=trial_design]{trial_design()}} and then
\code{\link[=slope_sample_size]{slope_sample_size()}} or \code{\link[=slope_power]{slope_power()}} directly. Those separate the two
stages of the calculation, so the mixed model is fitted once and any number of
designs evaluated against it; they express the visit schedule in real time
rather than through the \code{scale} workaround; and they ask one question each,
rather than switching between sample size and power depending on which
argument was left out.
}
\section{Differences from the Stata command}{


The interface is faithful, but three defects in the original are deliberately
not reproduced. The \code{dropouts} length check is enforced here; in the Stata code
the counter that drives it is built with a malformed macro reference, and an
over-long list can silently reduce the completer weight and inflate the
estimated sample size. The check that dropout proportions do not exceed 100\% is
applied with a tolerance; the original accumulates by repeated subtraction, so
\verb{dropouts(0.3 0.3 0.4)} fails despite summing to exactly 1. And a malformed
string in the original aborts the command instead of warning when \code{treat()} is
given with observational data; here it warns, as intended.

The \code{schedule} restriction to ascending integers of at least 1 is also lifted,
since this port builds the covariance matrix at the requested times rather than
on a unit-integer grid. \code{scale} is therefore never necessary, and a message
notes as much when it is used.
}

\examples{
# The paper's first worked example, p.588. Observational data with no
# comparison group, follow-up at 1 and 2 years, powering against a treatment
# expected to remove a third of the decline. Gives N = 712.
slopepower(slpower1, "sdmt", "id", "visit", schedule = c(1, 2),
           obs = TRUE, nocontrols = TRUE, effectiveness = 0.33)

# p.594. A completed trial, powered against the effect it actually observed
# rather than an assumed proportion. The uneven schedule draws the message
# about `scale` described above; the Stata original would reject it outright.
slopepower(slpower3, "sdmt", "id", "visit", schedule = c(0.5, 2),
           rct = TRUE, treat = "treat", usetrt = TRUE)

}
\seealso{
\code{\link[=slope_sample_size]{slope_sample_size()}}, \code{\link[=slope_power]{slope_power()}}
}
