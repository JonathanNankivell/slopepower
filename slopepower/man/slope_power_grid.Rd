% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/grid.R
\name{slope_power_grid}
\alias{slope_power_grid}
\title{Compare the power of many candidate trial designs}
\usage{
slope_power_grid(
  params,
  visits,
  dropout = NULL,
  n,
  effectiveness = 0.25,
  target = c("effectiveness", "observed"),
  alpha = 0.05
)
}
\arguments{
\item{params}{A \code{slope_params} object.}

\item{visits}{A numeric vector of visit times, or a named list of such vectors.
Each must begin at 0.}

\item{dropout}{\code{NULL}, a numeric vector of incremental proportions, a
\code{\link[=dropout_rate]{dropout_rate()}} object, or a named list mixing any of these.}

\item{n}{Total number of participants, held constant across the grid.
Required.}

\item{effectiveness, target, alpha}{Passed to \code{\link[=slope_power]{slope_power()}} and held constant
across the grid.}
}
\value{
A data frame with one row per design and dropout combination, with
columns \code{design}, \code{dropout}, \code{n_visits}, \code{n_follow_up}, \code{last_visit},
\code{dropout_total}, \code{n}, \code{n_per_arm}, \code{power}, \code{tte}, \code{var_tte} and
\code{effect_size}. \code{n} is constant; \code{power} is what varies.
}
\description{
Evaluates the cross product of a set of visit schedules and a set of dropout
assumptions against a single set of stage-one parameter estimates, at one fixed
sample size, returning one row per combination. This is the calculation behind
table 1 of Nash et al. (2021), which explores how adding interim visits changes
the power of a trial under different rates of loss to follow-up.
}
\details{
Because dropout proportions are supplied per visit, a single numeric vector
cannot describe the same withdrawal behaviour across designs with different
numbers of visits. Use \code{\link[=dropout_rate]{dropout_rate()}} to state the rate once and have it
expanded correctly for each schedule.

Use \code{\link[=slope_sample_size_grid]{slope_sample_size_grid()}} for the converse table: the sample size each
design needs to reach a target power.
}
\examples{
pars <- slope_params_manual(
  slope = -1.672, sigma2_intercept = 100, sigma2_slope = 2,
  sigma_cov = 5, sigma2_residual = 10
)
slope_power_grid(
  pars, n = 450, effectiveness = 0.33,
  visits  = list(final_only = c(0, 3), annual = 0:3, six_month = seq(0, 3, 0.5)),
  dropout = list(none = NULL, `5pc` = dropout_rate(0.05), `10pc` = dropout_rate(0.10))
)

}
\seealso{
\code{\link[=slope_power]{slope_power()}}, \code{\link[=slope_sample_size_grid]{slope_sample_size_grid()}}, \code{\link[=dropout_rate]{dropout_rate()}}
}
